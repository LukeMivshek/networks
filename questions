the 3 tag is not in our dhcp offer, so I can't set the default gateway
	we have tag 36, wiki says its a len 1 feild, were getting len 4, and morbius IP
	same thing in the ACK

what should routeInit be doing? Thinking net init should call routeInit

where does routenexthop come in
